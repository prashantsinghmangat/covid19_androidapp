<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covid19_androidapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Feedback" />
        <activity android:name=".patient_health_details" />
        <activity android:name=".Patient_registration_preview" />
        <activity android:name=".Patient_registration" />
        <activity android:name=".screen_patient_home" />
        <activity android:name=".Logout_navigation" />
        <activity android:name=".ProfileFragment" />
        <activity android:name=".finalsumit" />
        <activity android:name=".Homepage_nav"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".login_withoutcode"/>
            <activity android:name=".LoginPage"/>
                <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>